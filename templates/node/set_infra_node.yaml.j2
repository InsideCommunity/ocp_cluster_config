kind: Node
metadata:
  name: {{ node_name }}
  labels:
    node-role.kubernetes.io/infra: ""
{% if ocp_cluster_config_infra_node_taints | d(false) %}    
spec:
  taints: {{ ocp_cluster_config_infra_node_taints }}
{% endif %}