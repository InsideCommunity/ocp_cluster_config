---

# Identity Providers info
ocp_cluster_config_idp:
  # - name:
  #   secret:
  #   client_id:
  #   issuer:

# Proxy config
ocp_cluster_config_proxy:
  # http_proxy:
  # https_proxy:
  # no_proxy:
